# 2026-06-16 Summary

## MVP hardening

- Confirmed MCP servers are not needed for the contract-first MVP path. CE/Ghidra remain optional assist tooling.
- Added `examples/buggy_roguelike.py` as a real proof target with an intentional movement boundary bug.
- Added report generation from contract traces:
  - `report.json`
  - `report.html`
- Added CLI commands:
  - `python -m bridge_maker report --contract <dir>`
  - `python -m bridge_maker demo --out runs/grant_demo`
- Updated README and PRD to describe the SDK/Adapter-first MVP, not the old CoQ/socket path.
- Removed stale legacy artifacts:
  - CoQ Harmony mod folder,
  - old roadmap files,
  - old PRD archive,
  - checkpoint outputs,
  - stale generated state maps,
  - old CoQ/mock config and mock server.

## Verification

Commands run successfully:

- `python -m compileall bridge_maker src examples adapters`
- `python -m bridge_maker --help`
- `python -m bridge_maker demo --out runs/grant_demo`
- `python -m bridge_maker suggest --repo examples --out runs\grant_demo\suggestions_check`
- `python -m bridge_maker smoke --adapter examples/annotated_dummy.py --steps 20`
- `python -m bridge_maker report --contract runs\grant_demo`

`runs/grant_demo/report.json` now proves a working end-to-end MVP trace:

- Game: `buggy_roguelike`
- State fields: 5
- Actions: 4
- Oracles: 2
- Trace frames: 13
- Oracle hits: 3
- Status: `bug_found`

## Grant materials

Created:

- `grant_materials/bridge_maker_grant_presentation.pptx`
- `grant_materials/grant_demo_report.html`
- `grant_materials/grant_demo_report.json`
- `grant_materials/proof_of_work.md`
- `grant_materials/noita_ws_readiness.md`

The PPTX has 10 slides and opens via `python-pptx` validation. Full rendered slide QA was not possible because the official artifact-tool and LibreOffice/soffice are unavailable in this environment.

## External adapter readiness

- Inspected `probable-basilisk/noita-ws-api` at local commit `47054b0`.
- Confirmed the upstream bridge is raw Lua over WebSocket with JSON/raw replies and heartbeat messages.
- Added `adapters/noita_ws/README.md` as the honest next adapter path.
- Added an executable Noita WS adapter-boundary test with a fake Noita client.
- Live Noita integration remains pending the game/mod runtime, but the SDK architecture no longer depends on CE/Ghidra or binary guessing.
